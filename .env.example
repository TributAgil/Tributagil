# ─────────────────────────────────────────────────────────────
#  Supabase — expostas ao BROWSER (precisam do prefixo VITE_)
#  A chave `anon` é pública por natureza e deve ser protegida por RLS.
#  (O /api/gemini NÃO precisa de env própria de Supabase: a URL e a anon key
#   viajam no corpo da request e o isolamento vem do token do usuário + RLS.)
# ─────────────────────────────────────────────────────────────
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# hCaptcha do login (opcional — há fallback no código). PÚBLICO.
# O secret key correspondente vai SÓ no painel do Supabase:
#   Authentication -> Attack Protection -> Enable Captcha -> hCaptcha
# VITE_HCAPTCHA_SITEKEY=ac76753c-80fe-42c4-abe4-bd3a6fb236f2

# ─────────────────────────────────────────────────────────────
#  Gemini — usada SOMENTE no backend (/api/gemini). SEM prefixo VITE_,
#  para que a chave nunca seja embutida no bundle do frontend.
#  Aceita tanto o formato antigo (AIzaSy...) quanto o novo (AQ....).
# ─────────────────────────────────────────────────────────────
GEMINI_API_KEY=AQ....
# Opcional — modelo. Padrão: gemini-3.5-flash.
#   gemini-3.1-pro-preview   -> melhor qualidade; exige BILLING ativo no Google
#                               Cloud (no free tier retorna HTTP 429 / limit: 0)
#   gemini-3.5-flash-lite    -> mais barato/rápido, menos preciso
# GEMINI_MODEL=gemini-3.5-flash
# Opcional — temperatura da geração (padrão 0.3).
# GEMINI_TEMPERATURE=0.3
# Opcional — nível de "thinking" (modelos 3.x): high (padrão) | low | off.
#   Para a forma numérica dos modelos 2.5: GEMINI_THINKING_LEVEL=budget
#   junto de GEMINI_THINKING_BUDGET=512
# GEMINI_THINKING_LEVEL=high
# GEMINI_THINKING_BUDGET=512

# ─────────────────────────────────────────────────────────────
#  Envio de e-mail do formulário de suporte (/api/contato)
#  Sem RESEND_API_KEY, as mensagens são apenas registradas no log.
# ─────────────────────────────────────────────────────────────
# RESEND_API_KEY=re_...
CONTATO_EMAIL_TO=contato@tributagil.online
CONTATO_EMAIL_FROM=TributÁgil <no-reply@tributagil.online>
